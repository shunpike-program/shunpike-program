[toolchain]
anchor_version = "0.31.1"

[features]
resolution = true
skip-lint = false

# The deployed program id — matches declare_id! in
# programs/shunpike_stream/src/lib.rs and the deploy keypair.
[programs.localnet]
shunpike_stream = "GooHiyMEYnVAUkajiAc33fA9rQjF5K8ZvFojHNzhrShg"

[programs.devnet]
shunpike_stream = "GooHiyMEYnVAUkajiAc33fA9rQjF5K8ZvFojHNzhrShg"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
# The whole suite is Rust (unit + LiteSVM integration) — no ts-mocha layer.
# `anchor build` must run first so tests can load target/deploy/shunpike_stream.so.
test = "cargo test"
